//함수

// function bok(main) {
//   console.log(`${main} 볶음밥`);
// }

// bok("새우");
// bok("제육");

function sum(a, b) {
  console.log(a + b);
}
sum(10, 30);

//인자=함수의 입력 값=arguments
//매개변수=함수의 입력변수=parameter
//변수를 사용할 수 있는 범위가 중괄호로 감싸진 블록 안으로 제한, 외부에서 불러올 수 없음(조건문, 반복문도 마찬가지)

//default parameter 기본값
//arguments
